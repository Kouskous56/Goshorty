{
  "name": "goshorty-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser end-to-end tests for the GoShorty SPA (Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:local": "playwright test --project=local-chrome"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}